<div class="container">
  <div class="row">
    <div class="col-md-8 col-md-offset-2">
      <div class="row">
        <div class="row">
          <div class="col-md-5">
            <h2><%= link_to "Create Rent Post", new_rent_path, class: "btn btn-primary" %></h2>
          </div>

          <div class="col-md-7">
            <% @rents.each do |rent| %>
              <h2><%= link_to rent.title, rent_path(rent) %></h2>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
